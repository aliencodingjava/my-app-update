<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Shadow Layer -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="20dp" />
            <solid android:color="@color/material_yellow"/> <!-- 🔥 Semi-transparent black for shadow -->
        </shape>
    </item>
    <!-- Main Card Background Layer -->
    <item android:left="2dp" android:right="2dp" android:top="2dp" android:bottom="4dp">
        <shape android:shape="rectangle">
            <corners android:radius="20dp" />
            <solid android:color="@color/box_floating_bar"/> <!-- 🔥 Your Floating Bar Color -->
        </shape>
    </item>
</layer-list>
