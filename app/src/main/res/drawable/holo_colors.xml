<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"> <!-- or #33000000 for semi-transparent ripple -->

    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@color/holo_yellow_light"/> <!-- Optional mask, white makes full ripple visible -->
        </shape>
    </item>

    <item>
        <layer-list>
            <!-- Background -->
            <item>
                <shape android:shape="rectangle">
                    <size android:width="16dp" android:height="16dp"/>
                    <solid android:color="@color/box_qrcode"/>
                    <corners android:radius="30dp"/>
                </shape>
            </item>

            <!-- Foreground Fill -->
            <item android:id="@+id/fill">
                <clip android:clipOrientation="horizontal" android:gravity="left">
                    <shape android:shape="rectangle">
                        <size android:width="25dp"/>
                        <solid android:color="?attr/actionMenuTextColor"/>
                        <corners android:radius="30dp"/>
                    </shape>
                </clip>
            </item>

            <!-- Icon -->
            <item android:drawable="@drawable/palette_24dp_ffffff_fill1_wght400_grad0_opsz24" android:gravity="center"/>
        </layer-list>
    </item>
</ripple>
